# 도로명변환기 v1.12.1 — 주소 검색 대기 개선

주소 형태가 갖춰지면 빠르게 검색하고, 변환 결과는 정확히 검증하도록 검색 흐름을 개선했습니다.

## 변경 사항

- `동·리 + 번지` 또는 `도로명 + 건물번호`가 읽히면 사전 조회·높은 후보 점수·연속 인식을 기다리지 않고 원문 기본주소로 검색합니다.
- 이름·전화번호·건물명·상세주소를 모두 해석할 때까지 검색을 미루지 않습니다. 번호가 없는 부분 주소는 검색하지 않습니다.
- 생략된 지역은 기본 지역 설정으로 보완하며, 원문에 다른 지역이 명시되면 원문을 우선합니다.
- 사전 교정 후보는 원문 검색과 분리하고 ‘사전 후보 · 직접 선택’으로 표시합니다. 교정된 이름을 자동으로 검색·확정하지 않습니다.
- 지역·도로명 또는 동/리·산 여부·본번·부번의 정확 검증을 유지합니다. `5 → 5-1` 같은 유사 결과나 복수 일치 결과는 자동 확정하지 않습니다.
- 연속 스캔에서 기존 주소를 교체할 때는 새 주소의 연속 인식과 자동 검색 간격 제한을 적용합니다. 첫 검색은 지연시키지 않습니다.
- 같은 주소의 공백·줄바꿈 차이는 중복 요청으로 처리하며, 동·호수만 달라지면 기본주소 요청·캐시를 재사용합니다. 일치하지 않은 유사 후보 응답도 현재 실행 중 메모리 캐시에서 재사용합니다.
- 직접 고른 교정 주소가 동일한 OCR 오타 때문에 다시 바뀌지 않도록 했습니다. 일시정지·재개 시에도 수동 선택 보호를 유지합니다.
- 개발자 모드의 OCR 상태와 검색 상태를 분리했습니다. 검색 중·완료 표시가 다음 OCR 프레임 때문에 ‘주소 추출’로 되돌아가지 않습니다.
- 개발자 모드에 추출 기본주소와 실제 API 요청 주소를 구분해서 표시합니다. 수동 편집·부분 주소 선택으로 검색이 취소되거나 대기하는 상태도 표시합니다.
- 한 API가 통신·인증 오류를 반환했는데 다른 API에서 결과가 없다는 이유로 전체 검색을 ‘일치하는 주소 없음’으로 처리하던 경우를 수정했습니다.

## 검증

- 기존 ML Kit 한국어 OCR **16.0.1**, CameraX **1.4.2** 유지. 사전 데이터 및 모델은 변경하지 않았습니다.
- JVM 회귀 테스트 **104개 통과**. APK 빌드 검증 포함.
- 정적 검사 오류 0개·경고 62개. 기존 배포 APK와 서명 일치 확인.
- 원문 추출, 지역 생략·다른 지역 우선, 여러 줄 복원, 산·도로명 내부 숫자·본번/부번 보존, 전화번호·동호수 제외, 정확 지번 대조, 중복 요청·이전 응답 차단을 확인했습니다.
- 이전에는 사전 준비·후보 점수·동일 후보 2회 인식 이후 검색을 시작했지만, 이제 하나의 완성 원문 주소는 첫 인식에서 검색을 시작합니다.
- 고정 입력 시나리오에서 100개 반복·공백 차이·순간 오독 프레임에 대한 자동 검색 시작은 1회였습니다. 다음 주소로 교체할 때는 연속 2회 인식과 최소 1.2초 자동 검색 간격을 적용합니다. 사용자 직접 선택은 기다리지 않습니다.
- 오프라인·오류·지연 및 중복 응답 검증은 고정 데이터·가짜 요청·상태 테스트 기준입니다. 실제 API 호출량이나 휴대폰 인식 속도를 측정한 결과는 아닙니다.

## 참고 및 남은 한계

- 실제 사진 및 실기기 카메라·키보드·터치 조작은 이번 환경에서 검증하지 못했습니다.
- 한 프레임에 서로 다른 완성 주소가 여러 개 있으면 자동으로 첫 항목을 고르지 않고 직접 선택하도록 합니다.
- 잘못 읽힌 문자열도 주소 형태가 맞으면 검색을 시도할 수 있습니다. 다만 검색 응답과 입력 주소가 정확히 일치하지 않으면 성공으로 확정하지 않습니다. OCR이 다른 실제 주소로 잘못 읽는 경우까지 완전히 방지하는 기능은 아닙니다.
- 사진·OCR 원문의 자동 저장이나 진단용 외부 전송은 추가하지 않았습니다. 기존 주소 검색 API에는 추출한 기본주소를 전송합니다.
- 앱 패키지 `kr.co.addresslens`, 버전 `1.12.1`(24). 기존 배포본과 같은 서명으로 빌드합니다.
