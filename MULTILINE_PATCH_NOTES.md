# 도로명변환기 v1.12.2 — 여러 줄 주소 추출 수정

## 원인과 수정

- 기존 줄 연결은 다음 줄 전체가 번지여야 연결했고, `716-7 203호`처럼 상세주소가 함께 있으면 연결하지 못했습니다. 이제 주소명이 미완성일 때 가까운 다음 줄의 기본번호와 상세주소를 구분합니다.
- 도로명 복원 규칙이 한글 조각과 접미사 앞 글자를 요구해 `원선1` / `로 37`을 처리하지 못했습니다. 실제 관측된 숫자 포함 조각과 순수 접미사를 연결하도록 수정했습니다.
- `초지동` / `716-7 203호` → 검색 `경기도 안산시 단원구 초지동 716-7`, 상세 `203호`.
- `원선1` / `로 37 104동1202호` → 검색 `경기도 안산시 단원구 원선1로 37`, 상세 `104동 1202호`.
- `716-` / `7 203호`처럼 명시적인 하이픈이 있는 경우에만 관측된 부번을 복원합니다. 없는 숫자·문자를 만들지 않습니다.
- ML Kit 요소별 경계·좌표를 줄 연결 단계까지 사용합니다. 요소가 분리되어 있으면 번지와 호수를 분리하지만 `716-7203호`가 하나의 요소로 들어오면 임의로 쪼개지 않습니다.
- 다른 완성 주소, 다른 열, 먼 번호, 가능한 앞줄이 여러 개인 경우는 임의로 연결하지 않습니다. 연락처·이름·우편번호·배송 메모 등 표시된 정보 필드를 건너뛰어 번호를 붙이는 것도 막았습니다.
- 5자리 숫자를 일괄 삭제하지 않습니다. 같은 줄의 정상적인 기본번호는 유지하고, 별도 줄에 단독으로 나온 5자리 숫자는 우편번호와 구분이 필요하므로 수동 확인 후보로 남깁니다.
- 원문 기반 즉시 검색, 정확 주소 대조, 중복 요청·캐시·늦은 응답 차단, 수동 편집·연속 스캔 보호는 유지합니다. OCR 원문은 변경하지 않습니다.

## 화면 변경

- 하단 버튼 순서를 **왼쪽 주소 변환 / 오른쪽 다시 인식**으로 변경했습니다. 버튼 기능·크기·간격은 유지합니다. 연속 스캔일 때도 기존 일시정지·재개 기능은 해당 버튼에서 유지됩니다.
- 영역 변경 후에는 재인식 버튼 안내, 실제 처리 중에는 인식·추출 중, 종료 후 후보가 없으면 후보 없음으로 표시합니다. 실패 후에도 계속 ‘찾는 중’이라고 표시되던 문구를 수정했습니다.
- 개발자 모드에서 요소·줄 좌표, 연결·거부 이유, 기본주소와 상세주소를 확인할 수 있습니다. 기록은 최근 프레임의 메모리 내 진단으로 제한하며 파일 저장·외부 진단 전송은 추가하지 않았습니다.
- 잘린 진단 이미지를 ‘OCR 입력 영역이며 주소 검색 결과가 아님’으로 명시했습니다. 검색 상태는 OCR 상태와 독립적으로 유지합니다.

## 검증

- 수정 전: 실제 줄 연결 → 주소 추출 → 자동 검색 판단 경로에 추가한 A/B 재현 테스트 2개가 모두 실패했습니다. 기존 104개 테스트는 통과했습니다.
- 수정 후: **119개 JVM 회귀 테스트 통과**. 기본주소·호수, 산 지번, 숫자 도로명·접미사, 들여쓰기·다른 글자 크기, 요소 분리, 모호한 숫자, 전화번호·우편번호, 다른 주소 혼합 방지 및 상태 표시를 확인했습니다.
- 버튼 XML 순서 확인: `convertButton`, `scanAgainButton`.
- 엔진은 ML Kit 한국어 OCR `16.0.1`, CameraX `1.4.2` 그대로입니다. 모델·사전 데이터·API 인증 설정은 변경하지 않았습니다.

## 검증 범위와 한계

- 제공된 사진에서 확인한 OCR 문자열과 합성 좌표·요소로 재현했습니다. 사진 파일에 실제 ML Kit를 실행한 결과가 아닙니다.
- 연결된 Android 기기가 없어 실촬영·터치·키보드·기기별 화면 배치는 실기기 미검증입니다. 실제 API를 이용한 주소 존재 확인도 이번 재현 테스트에 포함하지 않았습니다.
- 숫자가 하나의 요소로 붙어 경계가 불명확하거나 줄 연결 대상이 여러 개인 경우에는 자동 추측하지 않습니다. 사용자의 후보 선택·부분 수정이 필요할 수 있습니다.
- 이름 등 표지가 없는 문자열은 실제 지명과 형태가 같을 수 있습니다. 모든 개인정보를 의미적으로 분류하는 기능이나 발송인·수취인 우선 판별은 추가하지 않았습니다.
- 기본주소 추출 성공은 실제 주소 존재 확인과 다릅니다. 변환 성공은 기존 API 정확 일치 검증 이후에만 표시합니다.
