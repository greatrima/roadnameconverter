# v1.12.0 정확 검증·정지 영역 선택

## 엔진 및 구현 범위

- 기존 내장 ML Kit 한국어 OCR `com.google.mlkit:text-recognition-korean:16.0.1`과 CameraX `1.4.2` 유지. 모델 교체·재학습 없음.
- 지번 정확 대조: 지역 경로·동/리·산·본번·부번. API의 첫 번째 유사 결과를 곧바로 성공으로 만드는 기존 경로 제거. 공급자가 명시한 관련 번지만 비교하며 ‘외 n필지’로 누락 번지를 추측하지 않음. 산 여부가 불명확한 관련 번지는 확정하지 않음.
- 정확 결과가 없거나 여러 주소가 일치하면 ‘일치하는 주소 없음’과 수동 선택 후보 표시. 수동 후보는 자동 확정 정책에서 제외하며 사용자가 실제 누른 항목만 적용.
- 공통 ViewPort의 원본 cropRect·0/90/180/270도 회전·fill-center 변환으로 live ROI 계산. 선택 영역 밖 픽셀 제외. 개발자 모드에서 실제 OCR 입력 이미지와 좌표 확인 가능.
- ML Kit 블록 경계에만 의존하지 않고 관측된 줄/요소의 위치·간격·주소 문맥으로 결합. 크기가 다른 줄, 도로명/행정명 중간 줄바꿈, 작은 기본주소 추출, 명시적인 건물명·동/층/호수 문맥 보존. 발송인·수취인 구분/우선순위는 추가하지 않음.
- 실시간 분석은 1920×1080급, 정지 촬영은 2560×1440급 해상도를 우선 요청하고 기기가 지원하는 조합으로 폴백. 원본 ROI를 먼저 처리하며 실패 시 동일 영역의 대비만 1회 보정. 실시간 보정 기회는 최소 2초 간격. 별도 흐림 판별 모델은 추가하지 않음.
- 정지 화면: 설정 기본 OFF·저장, 원본을 한 번 촬영하고 첫 영역을 한 번 인식. 이동·코너 조절·2손가락 확대. 변경 시 이전 후보·응답 무효화, 재인식은 명시적인 버튼으로 실행. 실패해도 정지 유지. 촬영 재개/뒤로가기 지원.
- 입력 포커스 및 편집 이후에는 제출 또는 다시 인식 전까지 OCR 덮어쓰기 방지. 같은 기본주소·다른 상세주소는 동일 API 요청/캐시를 공유하고 상세 표시만 갱신.
- 기존 안정화·요청 세대 번호·캐시 재사용. 추가로 최신 프레임 번호/유효 시간 확인, 이전 프레임 문자열 혼합 제거. 수동 모드는 하단 ‘다시 인식’, 연속 모드는 별도 ‘다시 인식’ 버튼으로 초기화.
- 개발자 모드 기본 OFF·저장. 원문/실제 처리 영역/추출/검색/실패 단계는 화면과 메모리에서만 사용. 사진·진단 원문 파일 기록, 로그 전송, 새로운 진단 서버 통신 없음. 주소 변환에는 기존과 같이 기본주소만 API로 전송.

## 검증

- JVM 회귀 테스트 90개 통과(기존 67 + 추가 23). 실제 조립 APK의 내장 사전·한국어 모델 검사 포함.
- `assembleDebug` 및 `lintDebug` 성공. APK 버전 1.12.0(23), 패키지 `kr.co.addresslens`, 이전 배포본과 서명 일치 확인.
- 5와 5-1, 산 여부, 지역/읍·면 차이, 명시된 관련 지번, 지역 생략 시 복수 일치, 수동 후보 자동 확정 차단.
- 4방향 회전, 원본 crop 원점 오프셋, 비율 차이의 fill-center 잘림, 확대·이동·코너 조절 역변환 및 범위 제한.
- 크기가 다른 인접 줄, 큰 건물명 아래 작은 기본주소, 좌우/상하 독립 주소 분리, 멀리 떨어진 번호 결합 금지.
- 광덕/동로, 상록/구 및 본오/동 복원, 102동 2/901호 복원, 건물번호와 다음 줄 호수 결합 금지, 전화번호의 지번 오인식 금지.
- 상세주소 변경의 별도 인식 키, 기본주소 요청 공유, 부분 편집·이전/중복 응답 차단. 오프라인 회귀 테스트 유지.
- 외부 주소 API로 진단용 사진·OCR 원문을 보내는 검증은 하지 않았습니다. 테스트는 고정 문자열/좌표/API 행 데이터와 가짜 요청으로 진행했습니다.

## 남은 검증·한계

- 연결된 Android 기기와 에뮬레이터가 없어 실제 사진에 대한 ML Kit 인식률·속도 비교, S10e/S25 Ultra/Fold 실촬영, 터치 확대·선택 상자 조작, 키보드/뒤로가기 시퀀스는 실기기 미검증입니다.
- 지번 응답의 관련 번지 정보가 제공되지 않으면 존재를 추정해 성공 처리하지 않습니다. 사용자 직접 선택이나 더 정확한 주소 입력이 필요할 수 있습니다.
- 공간 간격/문맥이 불명확하거나 인식되지 않은 글자는 임의로 복원하지 않습니다. 매우 흐리거나 기울어진 인쇄·복잡한 배치의 개선 정도는 실제 사진 비교가 필요합니다.
- 정지 이미지의 메모리 보관은 현재 화면 수명까지입니다. 앱 프로세스 종료 또는 화면 재생성 후 정지 사진을 복원하지 않습니다.
- 사진·원문 자동 저장/외부 진단 전송, 발송인·수취인 판별, 엔진 교체, 전국 건물 대응표 추가는 하지 않았습니다. 사전 데이터 자체는 이전과 동일합니다.

참고: [CameraX 변환 안내](https://developer.android.com/media/camera/camerax/transform-output), [ML Kit Android OCR](https://developers.google.com/ml-kit/vision/text-recognition/v2/android), [네이버 Geocoding 응답](https://api.ncloud-docs.com/docs/ai-naver-mapsgeocoding-geocode).
