# 도로명변환기 2.0.1

## 반복 업데이트 안내 수정

기존 v2.0.1 배포에 첨부된 `RoadNameConverter-menu-copy.apk`는 내부 버전이 2.0.0, 설치 번호가 26이었습니다. 업데이트 검사는 배포 페이지 이름이나 파일명이 아닌 APK 내부 버전을 사용하므로, 이 파일을 설치해도 v2.0.1을 새로운 버전으로 판단했습니다.

- APK 내부 버전과 설정 화면의 버전을 **2.0.1**로 맞췄습니다.
- Android 설치 번호(versionCode)를 **27**로 올렸습니다.
- 파일명은 `RoadNameConverter-v2.0.1.apk`입니다.
- 같은 2.0.1 배포에는 업데이트를 안내하지 않고, 이전 2.0.0은 업데이트할 수 있으며, 이후 2.0.2 이상은 정상적으로 새 버전으로 판단합니다.

## 포함 기능

- 설정 → 메뉴 → 버튼별 표시 ON/OFF 및 설정 저장
- 인식 주소·변환 주소 복사, 입력 수정 시 이전 변환 결과 복사 차단
- 작은 화면·큰 글자에 대응한 버튼 줄바꿈과 입력 화면 정리
- 수동 오프라인 모드, 볼륨키 카메라 확대·축소, 설정 자동 열기 제거

화면 변경 및 기존 검증 내용은 [메뉴·복사 패치 기록](MENU_COPY_PATCH_NOTES.md)을 참고하세요.

## 검증 범위

같은 배포 버전, 태그의 v/V 접두사, 이전 버전에서의 업데이트, 이후 새 버전 안내를 실제 Release JSON 처리 경로로 확인하는 회귀 테스트를 추가했습니다.

- 전체 자동 테스트 133개 통과, 실패·오류 0개.
- APK 빌드와 정적 검사 통과. 정적 검사 오류 0개, 경고 64개.
- 최종 APK의 실제 내부 버전 `2.0.1`, 설치 번호 `27` 확인.
- 기존 배포 파일과 동일한 서명 확인.
- APK SHA-256: `f61bb9f73b0c4331bb392cdc2589a338359236c5eb6d28867a2b1b61e459eaa1`.

실제 휴대전화 설치·카메라 조작은 자동 테스트에 포함되지 않습니다.
