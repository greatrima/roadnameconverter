# 도로명변환기 v2.0.0

## 주소 인식 및 검색 개선

- 번지 끝에 하이픈이 남아 있으면 미완성 번호로 판단합니다. OCR 요소를 묶으며 하이픈 앞에 공백이 들어가도 본번만 잘라 검색하지 않습니다.
- 가까운 다음 줄에 실제로 읽힌 부번이 있으면 연결합니다. 단위 없는 뒤쪽 숫자를 임의로 호수로 만들지 않으며, 멀리 떨어진 숫자·다른 열·전화번호·호수만 있는 줄은 연결하지 않습니다.
- 여러 줄로 나뉜 기본주소와 상세주소를 분리합니다. 번지 뒤에 호수나 층수가 있어도 기본주소 검색을 시작합니다.
- 숫자가 포함된 도로명과 다음 줄의 접미사를 복원합니다. 실제로 인식되지 않은 글자나 숫자는 만들지 않습니다.
- 완성된 원문 주소는 사전 조회를 기다리지 않고 검색하고, 최종 변환은 기존의 정확 일치 검증을 유지합니다. 유사 번지를 자동으로 정답 처리하지 않습니다.

## 화면 및 진단

- 하단 버튼은 왼쪽 ‘주소 변환’, 오른쪽 ‘다시 인식’ 순서입니다.
- 영역 변경 대기·OCR 처리·주소 추출 실패 안내를 실제 상태에 맞게 표시합니다.
- 개발자 모드에서 줄·요소 위치, 연결 및 거부 이유, 기본주소·상세주소를 확인할 수 있습니다. OCR 입력 이미지는 검색 결과와 구분합니다.
- 앱 이름은 ‘도로명변환기’, 버전은 `2.0.0`입니다. APK 파일명은 수식어 없이 `RoadNameConverter-v2.0.0.apk`를 사용합니다.

## 검증 및 한계

- 하이픈 앞 공백 때문에 본번만 검색되던 문제를 수정 전 테스트에서 재현하고 수정 후 통과했습니다.
- JVM 회귀 테스트 122개 통과. Android APK 빌드 및 정적 검사 수행.
- OCR 엔진: ML Kit 한국어 16.0.1, CameraX 1.4.2 유지. 내장 사전 및 기본 API 설정은 변경하지 않았습니다.
- 검증은 OCR 문자열·합성 좌표·요소를 이용한 자동 테스트입니다. 제공 사진을 ML Kit로 직접 실행하거나 실기기 촬영·조작을 검증한 것은 아닙니다.
- 번호 경계나 줄 연결이 불명확하면 자동 추측하지 않습니다. 원본에서 하이픈 자체가 인식되지 않은 경우까지 복원할 수는 없습니다.

## 공개 소스

- Android 앱 소스, 리소스, 내장 사전, 빌드 구성, 테스트, 사전 갱신 도구를 포함합니다.
- 개인 API 인증정보, 로컬 경로 설정, 서명키, 사용자 사진, 작업용 인수인계 문서는 제외합니다. 직접 빌드할 때는 본인의 API 정보를 사용해야 합니다.
