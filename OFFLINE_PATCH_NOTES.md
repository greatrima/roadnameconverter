# v1.11.4 오프라인 인식 패치

## 변경

- 기존 CameraX/ML Kit OCR, 내장 사전, 후보 추적기는 재사용했습니다.
- 오프라인이면 새 주소 변환 요청을 보내거나 대기하지 않습니다. 후보 표시 옵션이 꺼져 있어도 오프라인 후보와 선택한 주소를 보여줍니다.
- 통신 실패를 ‘인식 실패’로 보이게 하지 않고 ‘주소 인식 완료 · 변환 대기’와 실제 주소 미확인 상태를 표시합니다. 확인되지 않은 주소의 지도 버튼은 활성화하지 않습니다.
- 연결이 돌아와도 임의로 새 검색을 시작하지 않습니다. ‘주소 변환’을 누르면 유지된 주소를 재시도합니다.
- 연결 중단·다른 후보 선택·다시 스캔 이후의 오래된 응답과 중복 응답은 결과를 덮어쓰지 않습니다.
- 사전 최초 갱신은 연결이 있을 때 진행합니다. 오프라인으로 시작했다가 연결된 경우에도 최초 갱신을 다시 시도합니다. 사전 갱신은 인식의 선행 조건이 아닙니다.
- 도로명·지명 유사도 교정에서 숫자를 삭제·추가·변경하지 않도록 수정했습니다. 유사한 글자 후보와 실제로 확인된 건물 주소는 구분합니다.
- 수동 다시 스캔, 연속 스캔, 후보 선택, 네모칸 내부만 잘라 인식하는 기존 동작은 유지합니다.

## 검증

- 67개 JVM 회귀 테스트 통과: 기존 58개와 오프라인 흐름/숫자 보존/실제 APK 내장 모델·사전 검사 9개.
- 최종 lintDebug/assembleDebug 성공: 오류 0개, 경고 60개. APK 서명 검증 성공, 기존 v1.11.3과 동일한 인증서입니다.
- 실제 APK에서 한국어 OCR 모델 2개 이상 및 내장 사전 로딩 확인. 별도로 다운로드된 사전이 없는 경로에서 지역 선택, 상콕구→상록구 교정, 줄바꿈 복원, 상세주소 제외 확인.
- 가짜 네트워크/요청 대기열을 사용해 오프라인 시 원격 호출 0회, 중복 요청 합치기, 통신 중단 시 다른 API 추가 호출 방지, 재연결 후 재시도, 오래된/중복 응답 무시 확인. 테스트에 실제 API 키나 실제 주소 API 호출은 사용하지 않았습니다.
- 실제 휴대폰이 연결되지 않아 카메라 영상 OCR 및 비행기 모드 화면 조작은 실기기 미검증입니다.

## 남은 범위와 설치 후 확인

- 내장 사전 데이터는 변경하지 않았습니다. 소스 gzip 1,690,732바이트, APK 안 압축 해제 데이터 20,152,158바이트입니다.
- 사전은 명칭·지역 관계 데이터입니다. 건물별 지번↔도로명 대응표는 없으므로 새로운 주소의 실제 존재 확인과 변환은 인터넷이 필요합니다. 같은 앱 실행 중 이미 확인된 결과의 메모리 캐시는 재사용합니다.
- 새로 설치한 뒤 비행기 모드(Wi-Fi도 끄기)에서 지역 선택 → 스캔 → 후보 선택을 확인하세요. 통신 연결 후 ‘주소 변환’, 수동 다시 스캔, 연속 스캔의 다음 주소 전환도 확인하세요.
- 현재 주소를 조회하는 동안 통신을 끊었다가 다른 후보를 선택했을 때 이전 응답이 결과를 바꾸지 않는지 확인하세요.
- 이번 결과물은 Android APK입니다. GitHub에 자동 업로드하지 않았습니다.
