# 메뉴 표시 설정·주소 복사 패치

2026-09-17, 기존 패치 `ef42e71`의 코드를 기준으로 적용했습니다.
버전 이름 `2.0.0`, 버전 코드 `26`은 유지했습니다.

## 반영 내용

- 설정 → 메뉴 → 버튼별 표시 ON/OFF 화면을 추가했습니다.
- 지역 선택, 플래시, 지도에서 보기, 주소 변환, 화면 정지, 선택 영역 인식, 인식 주소 복사, 변환 주소 복사를 개별 설정합니다. 기본값은 모두 ON이며 기존 기능·기기·상태별 표시 조건은 유지합니다.
- 버튼 숨김은 기능 설정과 별도로 저장합니다. 앱 재시작·동일 서명 업데이트에서도 같은 저장소와 키를 사용합니다.
- 설정과 다시 인식은 고정 영역에 유지합니다. 연속 스캔의 일시정지/재개는 별도 버튼으로 분리했습니다. 정지 화면에서 다시 인식은 선택 영역을 재인식합니다.
- 촬영 재개·카메라 권한 허용은 기존 조건대로 표시합니다.
- 주소별 복사 아이콘을 추가했습니다. 인식 주소는 현재 입력값을 복사하므로 사용자가 수정한 내용과 오프라인·변환 실패 시 원문도 복사합니다.
- 변환 결과 복사는 성공한 결과만 사용합니다. 입력 변경, 새 검색, 실패, 초기화 시 이전 결과는 복사할 수 없습니다. 빈 값이나 상태 안내를 복사하지 않습니다.
- 복사 완료 안내를 표시합니다.
- 주소 구분선·제목·48dp 복사 버튼, 버튼 자동 줄바꿈, 숨김 시 공간 회수, 입력란 안내 중복 제거를 적용했습니다.
- 주소·결과 영역은 스크롤되며 설정과 다시 인식은 유지됩니다. 화면 높이에 맞춰 카메라 크기를 조정하고 키보드 표시 중에는 카메라 영역을 접습니다.
- 설정 자동 열기 제거와 볼륨키 줌 코드는 유지했습니다. 수동 오프라인 모드는 변환 응답뿐 아니라 요청 전에도 검사하도록 보완했습니다.

## 검증

- `testDebugUnitTest assembleDebug lintDebug`: 성공.
- 자동 테스트 129개 통과, 실패·오류 0개.
- 새 테스트: 주소 복사 상태 3개, 메뉴 기본값·설정 재생성 및 마이그레이션·버튼 줄바꿈 및 숨김·작은 화면/큰 글자 배치 4개.
- Android 리소스와 실제 글꼴 렌더링을 사용하는 Robolectric 화면 검증: 320×480에서 글자 배율 1배·2배, 360×760 기본 화면, 360×800 메뉴 화면. 생성 이미지로 복사 아이콘과 입력란을 확인했습니다.
- 기존 주소 파싱·후보·정확 검증·오프라인 처리·APK 내장 사전 회귀 테스트 포함.
- 정적 검사: 오류 0개, 경고 64개. 라이브러리 버전 안내, 미사용 리소스, 런처 아이콘, 배경 중복 그리기 등 경고는 남아 있습니다.
- 배포용 파일의 서명 검증 성공. PC의 기존 `RoadNameConverter-v2.0.0.apk`와 동일한 서명임을 확인했습니다.

## 로컬 검증 산출물 및 확인 범위

아래 파일은 로컬 빌드 결과이며 소스 저장소에는 포함하지 않습니다.

- APK: `../outputs/RoadNameConverter-menu-copy.apk`
- 테스트 보고서: `app/build/reports/tests/testDebugUnitTest/index.html`
- 화면 이미지: `app/build/reports/ui/`
- 빌드 로그: `build/build-verification.log`

실제 휴대전화의 카메라·볼륨키·터치·외부 지도 앱 연동과 설치 후 설정 유지는 기기에서 확인하지 못했습니다. 위 서명 비교는 PC의 기존 APK 기준입니다.
